# RSS Summarizer - Environment Variables Configuration
# Copy this file to .env and update the values below

# ============================================
# LM Studio Configuration (Required)
# ============================================
LM_STUDIO_URL=http://192.168.1.10:1234/v1/chat/completions
LM_STUDIO_MODEL=llama3

# ============================================
# General Settings
# ============================================
LOG_LEVEL=INFO
SUMMARY_MAX_CHARS=1800

# ============================================
# Feeds Configuration (JSON Array)
# ============================================
# Each feed must have: feed_name, url, webhook
# Optional: max_articles (defaults to 5)
# Optional: include_keywords (array of keywords to include, OR condition)
# Optional: exclude_keywords (array of keywords to exclude, OR condition) 
# Optional: keyword_match_mode ("title", "content", or "both" - defaults to "both")

FEEDS='[
  {
    "feed_name": "raspberry-pi",
    "url": "https://www.xda-developers.com/tag/raspberry-pi/feed/",
    "webhook": "https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN",
    "max_articles": 5,
    "include_keywords": ["AI", "機械学習", "Python"],
    "exclude_keywords": ["広告", "プロモーション"],
    "keyword_match_mode": "both"
  },
  {
    "feed_name": "tech-news",
    "url": "https://example.com/tech/rss",
    "webhook": "https://discord.com/api/webhooks/ANOTHER_WEBHOOK_ID/ANOTHER_WEBHOOK_TOKEN",
    "max_articles": 3
  }
]'

# ============================================
# Notes:
# ============================================
# - Replace YOUR_WEBHOOK_ID and YOUR_WEBHOOK_TOKEN with actual Discord webhook URLs
# - feed_name must be unique and will be used for processed file names (processed_feed-name.json)
# - max_articles: maximum number of articles to process per run for each feed
# - include_keywords: articles must contain at least one of these keywords (case insensitive)
# - exclude_keywords: articles containing any of these keywords will be skipped (case insensitive)
# - keyword_match_mode: where to search for keywords - "title", "content", or "both"
# - JSON format: use single quotes around the entire FEEDS value, double quotes inside JSON
# - To add more feeds, add more objects to the FEEDS array